<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <nav>
        <% if(backLink) { %>
            <a href="<%= backLinkUrl %>" id="back-link"><%= backLink  %></a>
        <% } %>
        <h2><%= heading %></h2>
        <% if (headerLink) { %>
            <a href="<%= headerLinkUrl %>" id="new-post"><%= headerLink %></a>
        <% } %>
        
    </nav>